{
  "_comment": "Skills in skills/manifest.json deliberately NOT bundled into openclaw.plugin.json (Tier B: host-repo only). This file is the machine-readable curation record: test/openclaw-plugin-manifest.test.ts asserts manifest = plugin + exclusions with no overlap. Removing a line here requires adding the slug to openclaw.plugin.json in the same commit (and vice versa). Seeded 2026-08-11 from the pre-wave state; each entry is a recorded decision, not an accident.",
  "exclusions": {
    "field-weekly": "brains-port fork-local personal skill (this user's feeds/research stack); not distributed via the plugin bundle",
    "fintwit-analyst": "brains-port fork-local personal skill (this user's feeds/research stack); not distributed via the plugin bundle",
    "frontmatter-guard": "pre-wave curation (recorded as-is; revisit individually)",
    "gbrain-upgrade": "host binary upgrade flow",
    "group-watch": "brains-port fork-local personal skill (this user's feeds/research stack); not distributed via the plugin bundle",
    "migrate": "host-side data-migration workflows; not useful to a downstream plugin install",
    "schema-author": "schema-pack authoring is a brain-owner activity, not a client activity",
    "schema-unify": "schema unification is a brain-owner migration activity",
    "setup": "host install/provisioning flow; plugin installs arrive already set up",
    "skill-optimizer": "requires host-side skillopt engine access and benchmark files",
    "smoke-test": "host-runtime health checks (container/daemon assumptions)",
    "todo-suggester": "brains-port fork-local personal skill (this user's feeds/research stack); not distributed via the plugin bundle",
    "weekly-reflection": "brains-port fork-local personal skill (this user's feeds/research stack); not distributed via the plugin bundle",
    "what-needs-me": "brains-port fork-local personal skill (this user's feeds/research stack); not distributed via the plugin bundle"
  }
}
